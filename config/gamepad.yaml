gamepad_node:
  ros__parameters:
    # EasySMX axes
    throttle_axis: 1   # left stick Y
    steering_axis: 3   # right stick X

    # Set to -1 to invert; caps belong in throttle.yaml.
    throttle_sign: 1
    steering_sign: 1
